import { document } from "postcss";
import useDocTitle from "../hooks/useDocTitle";

const Work = () => {
  useDocTitle("Work - Che's Website");

  return (
    <h1>You've reached the Work page!</h1>
  )
}

export default Work;
